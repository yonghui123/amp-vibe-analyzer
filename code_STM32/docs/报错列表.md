# 报错与已知问题列表

> 阶段 6 联调记录（2026-07-04）

## 已解决

| 项 | 说明 |
|----|------|
| AcqTask 跨线程调 LVGL | 已改 `GUI_PostRealtimeData` + `lv_async_call`（STM32） |

## 待板端验证

| 项 | 说明 |
|----|------|
| T1.2~T1.5 | 8 路 raw、采样率、Stop 停计数 |
| T3.1~T3.4 | 帧率 ~50/s、长时间稳定性 |
| T4.1 / T5.1 | 真实信号与 PP 对比 |
| §13.3 | 30min 无崩溃 |

## 已知限制（首期接受）

| 项 | 说明 |
|----|------|
| 通道 Save 后曲线条数 | 需切换 Tab 或重进主监控以重建 chart 系列（未做热重建） |
| 默认采集 JSON | 若旧 `acq_settings.json` 中 `vibration_ch_id=1`，请 Apply 改为 5（Vib_X） |
| 报警 | 阶段 8 前恒 Normal，Reset 按钮无报警时不显示 |

## 编译备注

| 项 | 说明 |
|----|------|
| PC stub_config_store | `atoi`/`atof` 隐式声明 warning（不影响运行） |
